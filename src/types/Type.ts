export interface ITask {
    id: string;
    name: string;
    description: string;
    priority: string;
  }
  export interface ITodos{
    userId: number,
    id: number,
    title: string,
    completed: boolean,
  }